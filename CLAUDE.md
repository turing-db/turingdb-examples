# TuringDB Examples

## Validate notebooks

Run all example notebooks end-to-end:

```
bash run_notebooks.sh
```

Edit the `NOTEBOOKS` array at the top of `run_notebooks.sh` to add or remove notebooks.
